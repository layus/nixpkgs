diff --git a/dependencies/NuSMV/Makefile b/dependencies/NuSMV/Makefile
index b5b5c2d..839394e 100644
--- a/dependencies/NuSMV/Makefile
+++ b/dependencies/NuSMV/Makefile
@@ -36,6 +36,7 @@ nusmv.unpacked:
 nusmv.patched: nusmv.unpacked
 	patch -p0 -N < pynusmv.patch
 	patch -p0 -N < cudd.patch
+	sed -e 's/union wait/int/g' -i NuSMV-2.5.4/cudd-2.4.1.1/util/pipefork.c
 	touch nusmv.patched
 
 # This target configures nusmv for proper compilation on your system
@@ -50,8 +51,6 @@ nusmv.configured: nusmv.patched
 															--with-zchaff-libdir=$(ZCHAFF) \
 															--with-zchaff-incdir=$(ZCHAFF) \
 															--with-pic \
-															--enable-static \
-															--enable-static-link \
 															CFLAGS="-g -O2 -fPIC" \
 														  CXXFLAGS="-g -O2 -fPIC"
 	touch nusmv.configured
diff --git a/dependencies/system_requirements.py b/dependencies/system_requirements.py
index 07a3514..6a69291 100644
--- a/dependencies/system_requirements.py
+++ b/dependencies/system_requirements.py
@@ -104,7 +104,7 @@ def check_requirements():
     Checks whether all the required utities are installed on the target host
     '''
     success  = check_required_tools()
-    success &= check_required_libs()
+    #success &= check_required_libs()
 
     if not success:
         error("Some of the system requirements are not satisfied on your machine")
